// Generated by `yarn new:mutationMock`
import { server } from '../../server'
import { loginMutation } from '../login';
import { type Options } from './type'

export const setLoginMutation = (options: Options) => {
  server.use(loginMutation(options))
}
